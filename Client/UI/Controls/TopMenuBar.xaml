<UserControl x:Class="BayMax.UI.Controls.TopMenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800"
             Focusable="True"
             MouseLeftButtonDown="OnMenuBackgroundClick">
    <Border Background="#252526" Padding="15,5" BorderBrush="#3E3E42" BorderThickness="0,0,0,1">
        <Grid>
            <StackPanel x:Name="State0_Menu" Orientation="Horizontal" Visibility="Visible">
                <Button x:Name="AddUINodeBtn" Content="+ UI Нода" Click="AddUI_Click" Padding="10,5" Margin="0,0,10,0" Cursor="Hand"/>
                <Button x:Name="AddLogicNodeBtn" Content="+ Логика" Click="AddLogic_Click" Padding="10,5" Margin="0,0,10,0" Cursor="Hand"/>
                <Button Content="Обновить ноды" Click="RefreshNodes_Click" Foreground="#007ACC" Padding="10,5" Margin="0,0,10,0" Cursor="Hand"/>
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="5,0,15,0" Background="#3E3E42"/>
				<TextBlock Text="Машины:" Foreground="LightGray" VerticalAlignment="Center" Margin="0,0,5,0"/>

                <ToggleButton x:Name="DeviceMenuButton" Content="Найти в сети..." 
                  Padding="8,3" Cursor="Hand" Checked="DeviceMenuButton_Checked"/>

                <Popup IsOpen="{Binding IsChecked, ElementName=DeviceMenuButton}" 
       StaysOpen="False" PlacementTarget="{Binding ElementName=DeviceMenuButton}"
       AllowsTransparency="True" PopupAnimation="Slide">
                    <Border Background="#252526" BorderBrush="#3E3E42" BorderThickness="1" Padding="5" Width="220" MinHeight="50">
                        <Grid>
                            <TextBlock Text="Машин в сети не обнаружено" 
                       Foreground="#666666" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       FontStyle="Italic"
                       TextAlignment="Center"
                       Margin="10">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=NetworkDevicesList, Path=HasItems}" Value="False">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <ItemsControl x:Name="NetworkDevicesList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border x:Name="DeviceItemBorder" BorderBrush="#3E3E42" BorderThickness="0,0,0,1" Padding="8,5" Background="Transparent">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Text="{Binding StatusIcon}" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14"/>

                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock x:Name="NameText" Text="{Binding Name}" Foreground="#DDD" FontWeight="Bold"/>
                                                    <TextBlock x:Name="IpText" Text="{Binding Ip}" Foreground="#888" FontSize="10"/>
                                                </StackPanel>

                                                <Button x:Name="ActionButton" Grid.Column="2" Width="26" Height="26" 
                                        Cursor="Hand" Click="ToggleDeviceInProject_Click" Tag="{Binding}">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Content" Value="+"/>
                                                            <Setter Property="Background" Value="#3E3E42"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsInProject}" Value="True">
                                                                    <Setter Property="Content" Value="-"/>
                                                                    <Setter Property="Foreground" Value="#FF4B4B"/>
                                                                    <Setter Property="ToolTip" Value="Убрать из проекта"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </Grid>
                                        </Border>

                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsInProject}" Value="True">
                                                <Setter TargetName="DeviceItemBorder" Property="Background" Value="#2D2D30"/>
                                                <Setter TargetName="NameText" Property="Foreground" Value="#A7FC00"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                </Popup>

                <Button Content="Очистить холст" Foreground="#FF4B4B" Padding="10,5" Margin="0,0,10,0" Cursor="Hand"/>

                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="5,0,10,0"/>
                <Button x:Name="DeployButton" Content="ДЕПЛОЙ" Click="Deploy_Click" 
        Background="#007ACC" Foreground="White" FontWeight="Bold" Padding="15,5" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>

            <StackPanel x:Name="State1_Menu" Orientation="Horizontal" Visibility="Collapsed">
                <TextBlock Text="Выбрана 1 нода:" Foreground="LightGray" VerticalAlignment="Center" Margin="0,0,15,0"/>
                <Button Content="Дублировать (WIP)" Padding="10,5" Margin="0,0,10,0" Cursor="Hand"/>
                <Button Content="Удалить" Click="Delete_Click" Foreground="#FF4B4B" Padding="10,5" Margin="0,0,10,0" Cursor="Hand"/>
            </StackPanel>

            <StackPanel x:Name="StateMulti_Menu" Orientation="Horizontal" Visibility="Collapsed">
                <TextBlock x:Name="MultiSelectText" Text="Выбрано нод: 0" Foreground="LightGray" VerticalAlignment="Center" Margin="0,0,15,0"/>
                <Button Content="Создать группу (WIP)" Padding="10,5" Margin="0,0,10,0" Cursor="Hand"/>
                <Button Content="Удалить выделенные" Click="Delete_Click" Foreground="#FF4B4B" Padding="10,5" Margin="0,0,10,0" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>